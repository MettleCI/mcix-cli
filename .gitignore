_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

# Ignore these draft folders until they're ready for publication
# They can be developed locally, and viewed by running this directory in
# a container, but we don't want to publish them until they're ready.
_pages/flow-analysis/
**/.DS_Store
